import { VNode, ComponentChild } from 'preact';
import { V as VNode$1 } from './types-0b3c4385.js';

declare const renderPreactScope: (vnode: VNode) => (el: HTMLElement | null) => HTMLElement;
declare const unwrap: (vnode?: ComponentChild) => VNode$1;

export { renderPreactScope as r, unwrap as u };
